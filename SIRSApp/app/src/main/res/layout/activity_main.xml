<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="pt.utl.ist.sirs.t05.sirsapp.MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="FileSystem Application"
        android:layout_centerHorizontal="true"
        android:id="@+id/main_activity_title"
        android:textSize="14sp"
        android:textStyle="normal|bold"
        android:fontFamily="sans-serif" />

    <Button
        android:text="Save"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="57dp"
        android:id="@+id/save_button"
        android:layout_alignParentBottom="true"
        android:layout_toEndOf="@+id/main_activity_title" />

    <TextView
        android:text="Enter password"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/insert_password_text"
        android:layout_above="@+id/password_box"
        android:layout_alignParentStart="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="10"
        android:id="@+id/password_box"
        android:layout_alignBottom="@+id/save_button"
        android:layout_alignParentStart="true"
        android:layout_marginStart="13dp" />

</RelativeLayout>
